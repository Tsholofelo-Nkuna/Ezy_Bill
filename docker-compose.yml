services:
  clientmanagement.presentation.web:
    image: clientmanagementpresentationweb:dev
    ports:
       - "5040:8080"
       - "5041:8081"  
    build:
      context: .
      dockerfile: ClientManagement.Presentation.Web/Dockerfile
    env_file:
      - .env
